package com.nikiz.data.local.entity

import androidx.room.Entity

@Entity(tableName = GalleryTag.TABLE_NAME)
class GalleryTag(

) {
    companion object {
        const val TABLE_NAME = "GalleryTag"
    }
}